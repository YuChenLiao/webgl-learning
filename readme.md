# 跟着大肥鱼老师（DeepSeek）学 WebGL

> 不讲玄学，从 GPU 管线和 GLSL 着色器讲起。

这个仓库是我跟着**大肥鱼老师**（DeepSeek）学 WebGL 的过程记录。它由两部分组成：一份从渲染管线讲到实战的**学习手册**，和一个把手册里的案例**亲手重写成工程化模块**的代码目录。

一句话定位：**WebGL 是 GPU 的遥控器，three.js 是坐在遥控器前面的导演。** 先把遥控器摸熟，再看导演是怎么用它的。

版本基准：WebGL 2.0 / GLSL ES 3.00，`gl-matrix` 3.4.4，对照 three.js r186。

## 仓库结构

| 路径 | 说明 |
| --- | --- |
| `learning-documents/WebGL学习手册.html` | 主教材，单文件 3400 行，9 章 + 24 道面试题，自带目录、代码高亮与进度条，双击即可在浏览器里读 |
| `L1/` | 案例 L1「第一颗三角形」的模块化实现，零依赖（不需要 `gl-matrix`），但同样要走 HTTP |
| `L3/` | 案例 L3「光照球体与轨道相机」的模块化实现，可直接运行 |
| `L4/` | 案例 L4「离屏渲染与后处理链」的模块化实现，可直接运行 |
| `readme.md` | 本文件 |

## 手册里有什么

手册是全部知识的主线，章节安排如下。

| 章节 | 内容 | 为什么值得看 |
| --- | --- | --- |
| 1 认识 WebGL | 技术定位、WebGL 1 还是 2、WebGPU 会不会取代它、完整的渲染管线全景 | 先建立"它到底在管什么"的边界感 |
| 2 环境与工具 | 最小可运行骨架、上下文参数逐个说明、Spector.js 等调试工具、矩阵库选型 | 上下文参数没配好，后面全是玄学 bug |
| 3 学习路线与自检 | 三条按目标划分的路线、四周推进表、能力自检表 | 用来决定"我该学到哪一步" |
| 4 核心概念详解 | 4.1 渲染管线 → 4.2 上下文与坐标系 → 4.3 着色器与 GLSL → 4.4 缓冲区与顶点属性 → 4.5 矩阵与空间变换 → 4.6 纹理 → 4.7 光照与法线 → 4.8 深度、剔除与混合 → 4.9 帧缓冲与后处理 → 4.10 交互与拾取 | 全书主体，13 处「概念补给」穿插解释顶点、图元、插值与重心坐标这类前置知识 |
| 5 案例：由简到深五个可运行项目 | **L1** 第一颗三角形 → **L2** 纹理立方体与 MVP → **L3** 光照球体与轨道相机 → **L4** 离屏渲染与后处理链 → **L5** 实例化粒子与点击拾取 | 每个案例都是完整可运行代码，且给出"跑通后该做的实验" |
| 6 进阶专题 | 性能优化清单、常见效果的技术选型、迁移 WebGPU、与 three.js 的分工 | 从"能画出来"到"知道该用什么方案画" |
| 7 排错速查 | 12 类典型故障的现象 → 原因 → 排查顺序；外加通用的"三步定位法" | 黑屏、纹理全黑、深度错乱这三件事几乎人人必踩 |
| 8 面试题详解 | 24 道，分基础 / 进阶 / 原理场景三档，每题给结论、实质差异与加分点 | 见下方"面试准备" |
| 9 收官自检与延伸 | 8 条能力自检 + 与 three.js 的衔接 | 用来自测是否真的打通了 |

## L3：光照球体与轨道相机

L1和L2比较简单，直接复制代码即可运行观察，所以直接跳到L3

对应手册第 5 章的 L3 案例，区别在于：手册里为了便于阅读，给的是**单文件版**（HTML 里内联全部 JS）；这里我按职责拆成了模块，并顺手做了几处工程化处理。

| 文件 | 职责 |
| --- | --- |
| `L3/index.html` | 页面骨架，`importmap` 从 CDN 引入 `gl-matrix`，挂载 canvas |
| `L3/main.js` | 主流程：上下文、视口与 DPR、渲染状态、几何构建、交错 VBO + VAO、程序化棋盘纹理、uniform 赋值、渲染循环 |
| `L3/gl/gl.js` | GLSL ES 3.00 的顶点与片元着色器源码（模板字符串导出） |
| `L3/utils/ball.js` | 程序化生成球体几何：位置、法线、UV、索引 |
| `L3/utils/camera.js` | `OrbitCamera` 轨道相机：pointer 事件拖拽 + 滚轮指数缩放 |
| `L3/compile/compile.js` | 着色器编译与 program 链接，失败时抛错并清理 |

几个值得记住的数：

- 球体取 64 段经线 × 40 段纬线，顶点数 `(64+1) × (40+1) = 2665`，索引数 `64 × 40 × 6 = 15360`。2665 < 65535，所以索引用 `Uint16` 就够。
- 顶点数据交错成一个数组 `[x, y, z, nx, ny, nz, u, v]`，每顶点 8 个 float，stride 32 字节，三个属性的字节偏移分别是 `0 / 12 / 24`。
- 属性位置用 `getAttribLocation` 动态查询，不硬编码 `0/1/2`，避免链接器重排后错位。
- 光照是逐像素 Blinn-Phong：漫反射 + 高光（指数 96）+ 距离衰减 + 冷色边缘光 + 环境光，最后做 `pow(color, 1/2.2)` 伽马编码输出。
- 纹理是程序化生成的棋盘格（免外部图片依赖），额外画了两条彩色标记，方便观察球体旋转与 UV 分布；球面横向用了 `REPEAT`，纵向用 `CLAMP_TO_EDGE`。

## L4：离屏渲染与后处理链

L4 = L3 + 一层离屏处理。球体几何、光照着色器、轨道相机**一行没改**（`ball.js`、`camera.js` 与 L3 的 SHA256 完全一致），新增的只有三处：离屏目标、后处理着色器与全屏三角形、把渲染循环拆成两个 Pass。

`main.js` 不再承载任何绘制逻辑，只做装配与编排。

| 文件 | 职责 |
| --- | --- |
| `L4/index.html` | 页面骨架，`importmap` 从 CDN 引入 `gl-matrix` |
| `L4/main.js` | 纯装配 + 编排：建资源，驱动 Pass 1（场景 → 离屏纹理）与 Pass 2（后处理 → 屏幕） |
| `L4/gl/gl.js` | 四段 GLSL：场景的 `VERT`/`FRAG` + 后处理的 `POST_VERT`/`POST_FRAG` |
| `L4/render/renderTarget.js` | `createRenderTarget` 建 FBO，`disposeRenderTarget` 释放 |
| `L4/render/scenePass.js` | 场景 Pass：几何、交错 VBO/VAO、uniform 缓存，画进当前绑定的帧缓冲 |
| `L4/render/postPass.js` | 后处理 Pass：全屏三角形 + 色差/暗角/灰度，参数可运行时调 |
| `L4/utils/fullscreen-triangle.js` | 覆盖全屏的三角形，属性位置由调用方传入 |
| `L4/utils/checkerTexture.js` | 程序化棋盘格纹理（从 `main.js` 里抽出来的） |
| `L4/ui/postToggle.js` | 左上角的开关：L3 直出 ↔ L4 后处理链，含按钮状态与 `P` / 空格快捷键 |
| `L4/utils/ball.js`、`L4/utils/camera.js`、`L4/compile/compile.js` | 与 L3 逐字节相同，未做修改 |

两个 Pass 的状态切换就是这一关的全部难点：

| Pass | 做什么 | 状态要求 |
| --- | --- | --- |
| Pass 1 | 绑 FBO → 视口设为离屏尺寸 → 清颜色与深度 → 画球体 | **开**深度测试、**开**背面剔除 |
| Pass 2 | 绑屏幕 → 视口设为画布尺寸 → 画全屏三角形 | **关**深度测试、**关**背面剔除 |

一条贯穿整关的判断依据：**会被采样的用纹理，只当缓冲区用的用 renderbuffer。** 颜色附件要留给 Pass 2 采样，所以是纹理；深度只需要参与测试、不被采样，所以是 renderbuffer，更省内存。

几处值得留意的地方：

- **离屏目标只建一个。** 手册里建了两个（为乒乓预留），但 L4 是单级后处理链，第二个纯占一整块全屏显存；要做两级模糊时再扩成数组即可。
- **GL 状态是全局的。** Pass 2 关掉的 `CULL_FACE`，必须在下一帧的 Pass 1 重新打开，否则从第二帧起背面剔除就永久丢了。
- **全屏三角形的属性位置必须查出来传进去。** `createFullscreenTriangle(gl, attribLocation)`，不要沿用硬编码 `0` 的版本。
- **相机位置取 `camera.getPosition()`。** 本仓库的 `OrbitCamera.update()` 只写视图矩阵、不返回位置；实测它的返回值是 `undefined`，手册那行 `const cameraPos = camera.update(view)` 在这里会把 `undefined` 送进 `gl.uniform3fv`。
- **暗角没有直接用 `smoothstep(0.85, 0.25, d)`。** GLSL ES 3.00 规定 `edge0 >= edge1` 时结果是 undefined，各驱动不一定一致；改写成逐点等价的 `1.0 - smoothstep(0.25, 0.85, d)`。
- **`OFFSCREEN_SCALE` 控制离屏分辨率。** 默认 1；后处理对分辨率不敏感时降到 0.5，填充率直接省掉四分之三。

### 左上角的那个开关

页面左上角有个开关，在 **「L4 · 后处理链」** 与 **「L3 · 直出」** 之间切换，也可以按 `P` 或空格。

它不是"跳过渲染"的偷懒开关 —— 两种状态走的是**完全相同的两遍渲染**，唯一变量是 `uPostAmount` 这一个 uniform：为 1 时输出色差 + 暗角 + 灰度，为 0 时着色器直接 `texture(uScene, uv)` 输出离屏原图。切换瞬间有大约 90ms 的指数缓动，能看清效果是从哪里"长"出来的，而不是画面一闪。

这么设计的好处是：**画面差异可以确定全部来自后处理**，而不是来自路径变化、状态残留或资源重建 —— 想验证这一点，看 `main.js` 里 `postPass.setAmount()` 前后那几行就够了。

有一处差异是开关消不掉的，而它恰好最值得盯着看：**切到「L3 直出」时，球体边缘会比 L3 毛糙。** 因为 `antialias: true` 只对默认帧缓冲生效，离屏 FBO 不会自动继承 MSAA —— 这正是手册「后处理常见三个坑」里的第 ③ 条。要修得上 `renderbufferStorageMultisample` 渲染再 `blitFramebuffer` 解析，或改用 FXAA / SMAA。

## 怎么跑起来

**必须通过 HTTP 打开，不能双击 `index.html`。** 三个案例都用了 `type="module"`，`file://` 协议下模块脚本会被同源策略拒绝；L3、L4 还通过 `importmap` 从 jsdelivr CDN 拉 `gl-matrix`，更需要 HTTP。

在仓库根目录启动一个静态服务器：

```bash
npx serve .
# 或
python -m http.server 8080
```

然后访问 `http://localhost:3000/L1/`、`http://localhost:3000/L3/`、`http://localhost:3000/L4/`（`serve` 默认端口 3000）；把端口换成 `8080` 就走 Python 那条。也可以先 `cd L4` 再启动，那样访问根路径即可。

页面操作：**拖拽鼠标环绕观察，滚轮拉近拉远。** 相机极角被限制在 `(0.02, π-0.02)` 之间，距离被限制在 `1.6 ~ 30`，所以不会翻转、也不会穿进球体内部。L4 左上角还有一个后处理开关（或按 `P`），用来对照 L3 与 L4 的差别。

> 离线环境提醒：唯一的外部依赖是 `gl-matrix`，走的是 CDN。需要在无网环境开发时，把它换成本地文件路径即可，其余代码零依赖。

## 跑通后该做的实验

光跑出来不算学会，能**预测**调参后的画面变化才算。手册 L3 章节给了三处改动，值得逐个验证：

1. 把片元着色器里的高光指数 `96.0` 改成 `8.0`：高光会变得又大又糊，材质看起来像塑料。
2. 把 `uAmbientColor` 从 `0.06` 提到 `0.3`：暗部变灰、立体感下降，能直观感受环境光在"偷"对比度。
3. 把片元输出改成 `outColor = vec4(vUv, 0.0, 1.0);`：直接看到 UV 在球面上的分布，这是排查纹理问题最直观的一招。

L4 先点左上角开关（或按 `P`）来回切几次，感受后处理前后差在哪：暗角、边缘的红蓝色散、整体饱和度。想单独拆开每一项，可以在控制台实时调（页面把两个 Pass 挂在了 `window.__l4` 上）：

```js
__l4.setPostParams({ vignette: 0 });                        // 关掉暗角，对比四角是否变亮
__l4.setPostParams({ aberration: 0.03 });                   // 把色差推到夸张，看边缘的红蓝分离
__l4.setPostParams({ vignette: 0.65, aberration: 0.004 });  // 还原
__l4.setPostEnabled(false);                                 // 等价于点一下开关
```

想确认离屏链路本身是通的，把 `POST_FRAG` 的 `main` 体整个换成 `outColor = texture(uScene, vUv);`：画面应当与不加后处理时完全一致。一样，说明两遍渲染接得没错，差异全部来自后处理本身；不一样，问题在离屏目标而不在滤镜。

## 学习进度

| 阶段 | 内容 | 状态 |
| --- | --- | --- |
| L1 | 管线、上下文、着色器、缓冲区 | **已按模块化拆分** |
| L2 | 矩阵与坐标空间、纹理 | 手册 L2 单文件版可直接运行 |
| L3 | 光照、深度与混合、轨道相机 | **已按模块化拆分** |
| L4 | 帧缓冲、后处理、离屏渲染链 | **已按模块化拆分**，两个 Pass 各自成模块，后处理参数可运行时调 |

L2、L5 的完整代码目前只存在于手册第 5 章，后续会照 L1、L3、L4 的方式逐个重写成目录。

## 面试准备

作为一个以「面试准备」为顶层目录的项目，手册第 8 章的 24 道题是这条学习路径的落点。它的难度分档是：**基础**（必答不丢分）、**进阶**（区分度所在）、**原理与场景**（决定评级）。

手册给的临场一句话准备：WebGL 的考察核心是**"你能不能说清 GPU 在做什么"**。把渲染管线的四段（顶点处理 → 光栅化 → 片元处理 → 逐片元操作）、两次"除以 w"（透视除法）、一次"插值"（varying 传递）串起来讲，基本可以支撑一场 40 分钟的深聊。

配合第 9 章的自检表自查，能定位**黑屏、纹理全黑、深度错乱**这三类典型故障是底线要求。

## 相关资料

手册页脚交叉引用了同系列的其他几份资料：Three.js 手册、图形渲染面试题库（48 题）、跨端技术学习总览。当前仓库只放入了 WebGL 这一本。

底层规范与参考：

- [WebGL 2.0 Spec](https://registry.khronos.org/webgl/specs/latest/2.0/)
- [MDN WebGL API](https://developer.mozilla.org/en-US/docs/Web/API/WebGL_API)
- [GLSL ES 3.00 规范](https://registry.khronos.org/OpenGL/specs/es/3.0/GLSL_ES_Specification_3.00.pdf)

浏览器支持：WebGL 2 自 Chrome 56 / Firefox 51 / Safari 15 / Edge 79 起默认可用。
